# Default values for nginx.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image: 
  repository: us-docker.pkg.dev/searce-cloud-products/k8s-test-mp/nginx
  tag: latest

service:
  type: LoadBalancer
  port: 80

userInput: "Welcome Searce!"
